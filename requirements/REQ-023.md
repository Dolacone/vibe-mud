# REQ-023 - Items UI

## 情境

> 道具主分頁需要集中顯示玩家持有的 Item 與可用的加工操作，不重複共用 header 資訊。

## 行為

1. `道具` 必須顯示玩家 Inventory，以及 Active 與 Expired Item 的既有狀態與操作。
2. `道具` 不得重複顯示 header 已提供的 Resource 持有量。
3. `道具` 必須顯示後端回傳的 Convert methods 與 Craft recipes，並允許玩家執行對應操作。
