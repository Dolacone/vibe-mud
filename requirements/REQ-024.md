# REQ-024 - Character UI

## 情境

> 角色主分頁目前只需要顯示玩家名稱、改名入口與既有 Rest，不公開登入身分資料或未實作功能。

## 行為

1. `角色` 必須顯示後端回傳的目前 Player name。
2. `角色` 必須提供修改 Player name 的操作。
3. 既有 Rest 操作必須保留在 `角色`，直到後續 REQ 指定其他位置。
4. `角色` 不得顯示應用程式 user ID、Google 顯示名稱或 email。
5. `角色` 不得顯示尚未實作的裝備、技能或等級 placeholder。
