# REQ-004 — 休息行動

## 情境

> 已登入玩家需要透過最小遊戲行動消耗 AP，並看到更新後的狀態。

## 行為

1. 已登入玩家可以在前端看到 `rest` 行動。
2. 玩家執行 `rest` 成功後，系統扣除 1 AP。
3. `rest` 成功後，前端顯示扣除後的目前 AP。
4. 玩家沒有 AP 時，系統拒絕 `rest`，且 AP 保持不變。
5. 玩家重新整理或重新登入後，先前 `rest` 扣除的 AP 仍反映在目前 AP。
