# REQ-021 - Map UI

## 情境

> 地圖主分頁需要集中顯示玩家所在位置與可移動路徑，不承擔其他主分頁的內容。

## 行為

1. `地圖` 必須顯示玩家目前 Location。
2. `地圖` 必須顯示後端回傳的可抵達 Location、Route 與 AP 成本。
3. 玩家必須能在 `地圖` 對後端回傳的 Route 執行 Move。
4. `move` 被 Monster 攔截時，`地圖` 必須顯示戰鬥過程與結果，並保持玩家位於原本 Location。
