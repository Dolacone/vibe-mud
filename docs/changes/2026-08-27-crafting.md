---
title: "Crafting"
status: Implemented
created: 2026-08-27
doc_type: change
last_reviewed: 2026-08-27
source_paths:
  - docs/architecture.md
  - docs/schemas.md
  - docs/terminology.md
  - requirements/BEHAVIOR.md
  - requirements/REQ-009.md
  - internal/authapi/store.go
  - internal/authapi/store_test.go
  - internal/authapi/server.go
  - internal/authapi/server_test.go
  - web/src/auth.ts
  - web/src/App.tsx
  - web/src/App.test.tsx
  - web/src/auth.test.ts
req_ref: REQ-009
base_branch: main
scope: "Tracks the first fixed crafting recipe from Resource consumption through persistent Item output."
---

## Problem Statement

Players can collect and convert materials into typed Resources, but no Action consumes Resources to create processed Items.

## Recommended Direction

Add normalized backend-owned recipes with Resource inputs, optional Item inputs, and one explicit Item output. Expose every recipe at every Location. The frontend submits only a recipe identifier. The backend atomically applies its AP cost, all inputs, and its output.

## Key Assumptions

- The first recipe always succeeds when its requirements are met.
- Success probability remains outside this change.
- Existing Inventory storage can hold Wood Component quantities.
- Crafting recipes do not depend on Location.
- Crafting and conversion keep separate rule tables and Action paths.

## Acceptance Criteria

REQ-009 is the source of truth. The plan stage will copy each criterion into its owning task.

## MVP Scope / Not Doing

- No random crafting outcomes.
- No crafting skill modifiers.
- No building modifiers or requirements.
- No Item weight, durability, or use behavior.
- No batch crafting.

## Tasks

Dependency graph:

```text
Task 1: Crafting persistence and transaction
  -> Task 2: Crafting API
      -> Task 3: Frontend crafting client
          -> Task 4: Crafting UI
```

### Task 1: Persist crafting recipes and execute craft atomically

- [x] Add normalized recipe, Resource input, and Item input tables in `internal/authapi/store.go`.
- [x] Seed the Wood Component Item, recipe, and Wood Resource input.
- [x] Return every valid recipe at every Location with all backend-owned values.
- [x] Exclude and reject every recipe without a Resource input.
- [x] Atomically consume AP and every input before adding the output Item.
- [x] Preserve all player state after missing recipe, insufficient AP, or insufficient input failures.
- [x] Preserve existing player state when initializing and seeding the crafting schema.
- [x] Cover recipe loading, invalid recipes, multiple inputs, optional Item inputs, schema upgrade, success, accumulation, persistence, and rollback in `internal/authapi/store_test.go`.

Source files: `internal/authapi/store.go`

Acceptance criteria:

- REQ-009.1: 每個 recipe 必須具有穩定 identifier 與顯示名稱。
- REQ-009.2: 每個 recipe 必須定義基本 AP 成本。
- REQ-009.3: 每個 recipe 必須定義一種以上 Resource input。
- REQ-009.4: 每個 recipe 可以定義零種以上 Item input。
- REQ-009.5: 每個 recipe 必須明確定義 output Item 與 quantity。
- REQ-009.6: Recipe output 不能從 recipe 名稱推導。
- REQ-009.7: `Wood Component` recipe 必須消耗 10 Wood Resource 與 10 AP。
- REQ-009.8: `Wood Component` recipe 不消耗 Item input。
- REQ-009.9: `Wood Component` recipe 必須產出 1 個 `Wood Component` item。
- REQ-009.10: 玩家必須能在任何 Location 看到並選擇 `Wood Component` recipe。
- REQ-009.14: Inputs 與 AP 足夠時，recipe 必須成功。
- REQ-009.15: 目前 recipe 不使用隨機成功機率。
- REQ-009.16: 成功結果必須原子更新 AP、Resource 與 Inventory。
- REQ-009.17: 重複製作必須累加 `Wood Component` quantity。
- REQ-009.19: 重新登入後，玩家必須取得已保存的狀態。
- REQ-009.20: 任一 Resource input 不足時，`craft` 必須失敗。
- REQ-009.21: 任一 Item input 不足時，`craft` 必須失敗。
- REQ-009.22: AP 不足時，`craft` 必須失敗。
- REQ-009.23: 失敗時，所有玩家狀態必須保持不變。

### Task 2: Expose backend-owned crafting through the API

- [x] Add crafting recipes to authenticated player-state responses in `internal/authapi/server.go`.
- [x] Add `POST /api/actions/craft` with an exact recipe identifier payload.
- [x] Reject missing, unknown, duplicate, malformed, and extra input without changing state.
- [x] Log success and sanitized rejection outcomes with user ID, Action, reason, and request ID.
- [x] Cover response shape, success, failure state, whitelist validation, and log sanitization in `internal/authapi/server_test.go`.

Source files: `internal/authapi/server.go`

Acceptance criteria:

- REQ-009.1: 每個 recipe 必須具有穩定 identifier 與顯示名稱。
- REQ-009.2: 每個 recipe 必須定義基本 AP 成本。
- REQ-009.3: 每個 recipe 必須定義一種以上 Resource input。
- REQ-009.4: 每個 recipe 可以定義零種以上 Item input。
- REQ-009.5: 每個 recipe 必須明確定義 output Item 與 quantity。
- REQ-009.6: Recipe output 不能從 recipe 名稱推導。
- REQ-009.7: `Wood Component` recipe 必須消耗 10 Wood Resource 與 10 AP。
- REQ-009.8: `Wood Component` recipe 不消耗 Item input。
- REQ-009.9: `Wood Component` recipe 必須產出 1 個 `Wood Component` item。
- REQ-009.10: 玩家必須能在任何 Location 看到並選擇 `Wood Component` recipe。
- REQ-009.11: 玩家必須能透過前端執行 `craft` Action。
- REQ-009.12: 前端只能提交後端提供的 recipe identifier。
- REQ-009.13: 前端不能覆寫成本、inputs 或 output。
- REQ-009.14: Inputs 與 AP 足夠時，recipe 必須成功。
- REQ-009.16: 成功結果必須原子更新 AP、Resource 與 Inventory。
- REQ-009.18: 前端必須顯示後端回傳的最新玩家狀態。
- REQ-009.20: 任一 Resource input 不足時，`craft` 必須失敗。
- REQ-009.21: 任一 Item input 不足時，`craft` 必須失敗。
- REQ-009.22: AP 不足時，`craft` 必須失敗。
- REQ-009.23: 失敗時，所有玩家狀態必須保持不變。
- REQ-009.24: 後端必須拒絕不存在的 recipe identifier。
- REQ-009.25: 後端必須拒絕錯誤格式與未支援欄位。
- REQ-009.26: 後端必須記錄拒絕原因、user ID 與 request ID。
- REQ-009.27: Log 不得包含 credentials、session 或 OAuth 資料。

### Task 3: Parse recipes and craft results in the frontend client

- [x] Parse every recipe, its Resource and Item inputs, AP cost, and explicit output in `web/src/auth.ts`.
- [x] Submit only `recipe_id` to the craft endpoint.
- [x] Apply backend-authoritative state for success and every failure branch.
- [x] Reject malformed recipe and craft response contracts in `web/src/auth.test.ts`.

Source files: `web/src/auth.ts`

Acceptance criteria:

- REQ-009.1: 每個 recipe 必須具有穩定 identifier 與顯示名稱。
- REQ-009.2: 每個 recipe 必須定義基本 AP 成本。
- REQ-009.3: 每個 recipe 必須定義一種以上 Resource input。
- REQ-009.4: 每個 recipe 可以定義零種以上 Item input。
- REQ-009.5: 每個 recipe 必須明確定義 output Item 與 quantity。
- REQ-009.6: Recipe output 不能從 recipe 名稱推導。
- REQ-009.7: `Wood Component` recipe 必須消耗 10 Wood Resource 與 10 AP。
- REQ-009.8: `Wood Component` recipe 不消耗 Item input。
- REQ-009.9: `Wood Component` recipe 必須產出 1 個 `Wood Component` item。
- REQ-009.10: 玩家必須能在任何 Location 看到並選擇 `Wood Component` recipe。
- REQ-009.11: 玩家必須能透過前端執行 `craft` Action。
- REQ-009.12: 前端只能提交後端提供的 recipe identifier。
- REQ-009.13: 前端不能覆寫成本、inputs 或 output。
- REQ-009.18: 前端必須顯示後端回傳的最新玩家狀態。
- REQ-009.23: 失敗時，所有玩家狀態必須保持不變。
- REQ-009.24: 後端必須拒絕不存在的 recipe identifier。
- REQ-009.25: 後端必須拒絕錯誤格式與未支援欄位。

### Task 4: Display and execute the Wood Component recipe

- [x] Display the Wood Component recipe at every Location in `web/src/App.tsx`.
- [x] Show AP cost, every input, and the explicit output.
- [x] Execute craft by recipe identifier and prevent duplicate pending Actions.
- [x] Display the backend-authoritative AP, Inventory, and Resource state after success or failure.
- [x] Cover recipe display, any-Location access, success, failure, and pending behavior in `web/src/App.test.tsx`.

Source files: `web/src/App.tsx`

Acceptance criteria:

- REQ-009.1: 每個 recipe 必須具有穩定 identifier 與顯示名稱。
- REQ-009.2: 每個 recipe 必須定義基本 AP 成本。
- REQ-009.3: 每個 recipe 必須定義一種以上 Resource input。
- REQ-009.4: 每個 recipe 可以定義零種以上 Item input。
- REQ-009.5: 每個 recipe 必須明確定義 output Item 與 quantity。
- REQ-009.6: Recipe output 不能從 recipe 名稱推導。
- REQ-009.7: `Wood Component` recipe 必須消耗 10 Wood Resource 與 10 AP。
- REQ-009.8: `Wood Component` recipe 不消耗 Item input。
- REQ-009.9: `Wood Component` recipe 必須產出 1 個 `Wood Component` item。
- REQ-009.10: 玩家必須能在任何 Location 看到並選擇 `Wood Component` recipe。
- REQ-009.11: 玩家必須能透過前端執行 `craft` Action。
- REQ-009.12: 前端只能提交後端提供的 recipe identifier。
- REQ-009.13: 前端不能覆寫成本、inputs 或 output。
- REQ-009.14: Inputs 與 AP 足夠時，recipe 必須成功。
- REQ-009.15: 目前 recipe 不使用隨機成功機率。
- REQ-009.17: 重複製作必須累加 `Wood Component` quantity。
- REQ-009.18: 前端必須顯示後端回傳的最新玩家狀態。
- REQ-009.19: 重新登入後，玩家必須取得已保存的狀態。
- REQ-009.20: 任一 Resource input 不足時，`craft` 必須失敗。
- REQ-009.21: 任一 Item input 不足時，`craft` 必須失敗。
- REQ-009.22: AP 不足時，`craft` 必須失敗。
- REQ-009.23: 失敗時，所有玩家狀態必須保持不變。
- REQ-009.24: 後端必須拒絕不存在的 recipe identifier。
- REQ-009.25: 後端必須拒絕錯誤格式與未支援欄位。

## Review Issues

## Plan Review Issues

- [x] Task 1 says to return every recipe, but REQ-009.3 and `docs/schemas.md` require at least one Resource input. Add explicit validation and tests so a recipe without Resource inputs is never exposed or executed.
- [x] Task 1 does not cover upgrading an existing SQLite database. Add a test that opens pre-crafting player data, creates and seeds the new tables, preserves existing player state, and makes the recipe available.
